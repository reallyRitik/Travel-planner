export type Feature = {
  icon: string;
  title: string;
  description: string;
};

export type Benefit = {
  icon: string;
  title: string;
  description: string;
};

export type Step = {
  icon: string;
  title: string;
  description: string;
};